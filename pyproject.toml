[project]
name = "antigravity-quota"
version = "0.1.0"
description = "API Server for Antigravity Quota Management"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.127.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.1",
]
